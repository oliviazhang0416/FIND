# FIND 0.1.1

* Initial CRAN submission.
* Provides design constructors for 3plus3, i3plus3, g3, boin, and mtpi2 designs.
* Implements `get_decision()` for generating decision tables.
* Implements `decision_table()` for comparing decision tables across designs.
* Implements `run_simulation()` for running simulations.
* Implements `oc_plot()` for comparing operating characteristics.
